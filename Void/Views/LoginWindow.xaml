<Window x:Class="VoidTerminal.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Void Login" Height="300" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" Background="#1E1E1E"
        MouseDown="Window_MouseDown">
    <Border BorderBrush="#8A2BE2" BorderThickness="1">
        <Grid>
            <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Style="{StaticResource CloseBtnStyle}" Width="30" Height="30" Margin="5"
                    Click="BtnExit_Click"/>

            <StackPanel VerticalAlignment="Center" Margin="20">
                <TextBlock Text="ACCÈS SÉCURISÉ" Foreground="#8A2BE2" 
                           FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,20" FontWeight="Bold"/>

                <PasswordBox x:Name="TxtPassword" KeyDown="TxtPassword_KeyDown" Background="#2D2D30" Foreground="White" BorderThickness="0" Padding="10" Margin="0,0,0,20"/>

                <Button Content="CONNEXION" Click="BtnLogin_Click"
                        Background="#8A2BE2" Foreground="White" FontWeight="Bold"
                        BorderThickness="0" Padding="10" Cursor="Hand"/>
            </StackPanel>

            <Grid x:Name="CustomAlertOverlay" Background="#CC000000" Visibility="Collapsed" MouseLeftButtonUp="OnOverlayClick" Panel.ZIndex="100">
                <Grid Width="350" Height="170">
                    <Border Background="#1E1E1E" CornerRadius="10" BorderBrush="#FF5555" BorderThickness="1">
                        <StackPanel Margin="20" VerticalAlignment="Center">
                            <TextBlock x:Name="LblAlertTitle" Text="Accès refusé" Foreground="#FF5555" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                            <TextBlock x:Name="LblAlertMessage" Text="Mot de passe incorrect." Foreground="White" FontSize="14" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,25"/>
                            <Button x:Name="BtnAlertOk" Content="OK" Click="BtnCloseAlert_Click" Background="#FF5555" Foreground="White" Width="100" Height="35" BorderThickness="0" Cursor="Hand" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>

        </Grid>
    </Border>
</Window>